spring:
  application:
    name: user-center

  # DataSource Config
  datasource:
    url: jdbc:mysql://localhost:3306/user_center?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 10086

knife4j:
  enable: true
# http://localhost:10086/doc.html
